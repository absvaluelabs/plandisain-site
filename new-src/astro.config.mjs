// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://absvaluelabs.github.io',
  base: '/plandisain-site/new/',
  outDir: '../new',
  trailingSlash: 'always',
});
